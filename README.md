# Transfer-Learning-for-Sentiment-Analysis-using-DistilBERT
A sentiment analysis project using Transfer Learning with DistilBERT. The notebook prepares text data, tokenizes it with HuggingFace transformers, fine-tunes a pre-trained model, evaluates performance and saves the final classifier.
